# Среда разработчика для Debian/Ubuntu

Автоматическая установка:
- **VSCodium** — свободная IDE без телеметрии
- **Forgejo** — локальный Git-сервер (форк Gitea)
- Настройка пользователя `dev` для безопасной работы

> 💡 Работает на системах с `systemd`.

---

## 🔧 Требования

- любой Debian/Ubuntu с GUI
- Доступ от `root`
- Интернет

---

## 🚀 Установка

### 1. Установите `uv` и `ansible` через `pipx` (глобально, но безопасно)

```bash
# Обновите систему
sudo apt update && sudo apt upgrade -y
```
# Установите pipx (менеджер CLI-приложений Python)

```bash
sudo apt install -y pipx
python3 -m pipx ensurepath
```

# Установите современный Ansible (ansible-core) и uv
```bash
pipx install uv
pipx install ansible-core
```

# Настройте PATH для глобального использования (pipx автоматически настраивает PATH для установленных приложений)
```bash
source ~/.bashrc
```

# Проверьте
```bash
uv --version
ansible --version  # → ansible [core 2.19+]
```

# Скачайте плейбук (или создайте вручную)
```bash
curl -fsSL https://raw.githubusercontent.com/triglavfree/deploy/main/scripts/code-server/bootstrap.sh | sudo -E bash
```

# или Запустите
```bash
ansible-playbook setup.yml
```
